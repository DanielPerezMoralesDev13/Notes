// Author: Daniel Benjamin Perez Morales
// GitHub: https://github.com/DanielPerezMoralesDev13
// Email: danielperezdev@proton.me

/**
 * Just an Array
 * @type {Array<any>}
 */
const age = [18, 22, 17, 30, true, {}, "hello"];

/**
 * Array of Numbers
 * @type {Array<number>}
 */
const scores = [10, 19.9, 20.0, 33.3, 17.4];

/**
 * Arrays of Numbers and Strings
 * @type {Array<number|string>}
 */
const strnum = [10, 20, "30", 110, 100];

/**
 * Arrays of names (strings)
 * @type {Array<string>}
 */
const names = ["ryan", "joe", "carlos"];
// names.push(33); // Uncommenting this would break type correctness as 33 is a number.
